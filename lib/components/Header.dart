import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

PreferredSizeWidget Header(BuildContext context) {
  return AppBar(
    title: Text('Gerenciador de Estacionameto'),
    backgroundColor: Colors.green,
  );
}